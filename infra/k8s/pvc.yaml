apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: chatlite-data
  namespace: chatlite
  labels:
    app.kubernetes.io/name: chatlite
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
