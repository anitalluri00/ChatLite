apiVersion: v1
kind: Secret
metadata:
  name: chatlite-secret
  namespace: chatlite
  labels:
    app.kubernetes.io/name: chatlite
type: Opaque
stringData:
  CHATLITE_APP_SECRET: "replace-with-a-long-random-secret"
  # Optional PostgreSQL storage:
  # CHATLITE_DATABASE_URL: "postgresql://user:password@host:5432/chatlite"
