apiVersion: v1
kind: Service
metadata:
  name: chatlite
  namespace: chatlite
  labels:
    app.kubernetes.io/name: chatlite
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: chatlite
    app.kubernetes.io/component: web
  ports:
    - name: http
      port: 80
      targetPort: http
